# FastAPI core
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.25
aiosqlite==0.19.0
alembic==1.13.1
asyncpg==0.29.0          # PostgreSQL 异步驱动
psycopg2-binary==2.9.9   # PostgreSQL 同步驱动（Alembic 迁移用）

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # 锁定版本，bcrypt>=5.0 与 passlib 1.7.4 不兼容

# Data validation
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0

# WebSocket
websockets==12.0

# Gateway protocol adapters
pymodbus>=3.6,<4.0

# Tools
python-dotenv==1.0.0
apscheduler==3.10.4
scipy>=1.11.0  # 用于多设备协同优化算法
jinja2>=3.1.0  # 邮件模板渲染

# Data export
openpyxl==3.1.2

# PDF 报表生成
reportlab>=4.0
weasyprint>=60.0  # Markdown 转 PDF（误诊报告导出）
markdown>=3.5  # Markdown 解析

# 数值计算 (容量预测、负荷预测、调度优化)
numpy>=1.24.0

# 异常检测 (训练数据质量检测 - Story 26.9)
scikit-learn>=1.3.0,<2.0

# 图数据结构 (配电拓扑分析)
networkx>=3.0,<4.0

# Redis (异步)
redis>=5.0.0

# 监控指标
prometheus-client>=0.16.0

# Deep learning (optional) — 见 requirements-ml.txt
# pip install -r requirements-ml.txt

# HTTP client (联动引擎 Webhook，兼容 Starlette 0.35 TestClient)
httpx>=0.25.0,<0.28.0

# YAML 配置 (消防策略)
PyYAML>=6.0

# Evidence validation
jsonschema>=4.23,<5.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-timeout>=2.2.0

# Linting
ruff==0.15.2
