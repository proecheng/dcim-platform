description = 'Playtest session planner. Use when the user says "lets create a playtest plan" or "I want to schedule gameplay testing"'
prompt = """
Execute the BMAD 'gametest-playtest-plan' workflow.

CRITICAL: This is a structured YAML workflow. Follow these steps precisely:

1. LOAD the workflow definition from {project-root}/_bmad/gds/workflows/gametest/playtest-plan/workflow.yaml
2. PARSE the YAML structure to understand:
   - Workflow phases and steps
   - Required inputs and outputs
   - Dependencies between steps
3. EXECUTE each step in order
4. VALIDATE outputs before proceeding to next step

WORKFLOW FILE: {project-root}/_bmad/gds/workflows/gametest/playtest-plan/workflow.yaml
"""
