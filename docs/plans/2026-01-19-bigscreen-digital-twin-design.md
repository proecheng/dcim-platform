# 数字孪生机房大屏设计方案

**创建日期**: 2026-01-19
**版本**: V1.2
**状态**: 已确认

## 1. 概述

### 1.1 目标

设计一个基于数字孪生技术的机房动环监测大屏界面，使用 Three.js 实现3D机房可视化，支持多场景模式切换。

### 1.2 使用场景

- **指挥中心模式**: 24小时值班监控，固定视角，全数据展示
- **运维模式**: 日常运维使用，自由交互，可操作控制
- **展示模式**: 领导参观演示，自动巡航，视觉效果增强

## 2. 整体架构

### 2.1 页面布局

```
┌─────────────────────────────────────────────────────────────┐
│  顶部状态栏 (悬浮)                                            │
│  [时间] [机房名称] [模式切换] [全屏] [PUE: 1.45] [告警: 3]    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────┐                               ┌─────────┐      │
│  │ 左侧面板 │                               │ 右侧面板 │      │
│  │ 环境监测 │      Three.js 3D 场景         │ 能耗统计 │      │
│  │ 温度统计 │        (全屏背景)             │ PUE趋势 │      │
│  │ 湿度统计 │                               │ 功率分布 │      │
│  │ 告警列表 │                               │ 需量状态 │      │
│  └─────────┘                               └─────────┘      │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  底部控制栏 (悬浮)                                            │
│  [区域选择] [数据图层开关] [视角预设] [巡航/暂停] [时间轴]      │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 技术栈

| 模块 | 技术选型 |
|------|----------|
| 3D渲染 | Three.js |
| 后处理 | Three.js EffectComposer (辉光、景深) |
| 动画 | GSAP |
| 数据面板 | Vue 3 + Element Plus |
| 图表 | ECharts |
| 状态管理 | Pinia |

## 3. 3D场景设计

### 3.1 场景层级结构

```
Scene (场景根节点)
├── Environment (环境)
│   ├── AmbientLight (环境光)
│   ├── DirectionalLight (主光源)
│   ├── Floor (地板，带反射效果)
│   └── Skybox (深色科技风格背景)
│
├── DataCenterGroup (机房主体)
│   ├── Module_A (模块A区域)
│   │   ├── CabinetRow_1 (机柜排)
│   │   │   ├── Cabinet_001 (单个机柜)
│   │   │   │   ├── CabinetFrame (机柜框架)
│   │   │   │   ├── Devices[] (内部设备)
│   │   │   │   ├── StatusIndicator (状态指示灯)
│   │   │   │   ├── HeatmapOverlay (温度热力覆盖层)
│   │   │   │   └── DataLabel (功率/温度标签)
│   │   │   └── Cabinet_002...
│   │   ├── CoolingUnit (精密空调)
│   │   └── AirflowParticles (气流粒子动画)
│   ├── Module_B...
│   └── Module_C...
│
├── Infrastructure (基础设施)
│   ├── UPS_Room (UPS室)
│   ├── PowerDistribution (配电区)
│   └── CoolingSystem (制冷系统管道)
│
└── Overlays (叠加层)
    ├── AlarmBubbles (告警气泡)
    ├── SelectionOutline (选中高亮轮廓)
    └── NavigationPath (飞行路径线)
```

### 3.2 机房布局

采用模块化布局，支持多个独立模块区域，每个模块有自己的机柜组和空调。

### 3.3 3D模型方案

采用混合方案：
- **初期**: 程序化几何体生成（立方体机柜、圆柱管道等）
- **后续**: 可替换为精细3D模型文件

```typescript
interface CabinetConfig {
  id: string
  position: { x: number, y: number, z: number }
  size: { width: 0.6, height: 2.0, depth: 1.0 }  // 标准42U机柜尺寸(米)
  uCount: 42
  devices: DeviceSlot[]
  color: string
}
```

## 4. 数据可视化层

### 4.1 温湿度热力图

```typescript
const temperatureColorScale = {
  cold: '#0066ff',    // < 18°C 过冷(蓝色)
  cool: '#00ccff',    // 18-22°C 偏冷
  normal: '#00ff88',  // 22-26°C 正常(绿色)
  warm: '#ffcc00',    // 26-30°C 偏热
  hot: '#ff3300'      // > 30°C 过热(红色)
}
```

### 4.2 设备状态指示

| 状态 | 颜色 | 效果 |
|------|------|------|
| 正常运行 | `#00ff88` 绿色 | 机柜顶部指示灯常亮 |
| 告警 | `#ff3300` 红色 | 指示灯闪烁 + 光晕脉冲 |
| 离线 | `#666666` 灰色 | 机柜整体变暗 |
| 选中 | `#00ccff` 蓝色 | 边缘辉光轮廓线 |

### 4.3 气流动画

```typescript
interface AirflowConfig {
  coldAisle: {
    color: '#00ccff',      // 冷气流(蓝色)
    direction: 'up',
    speed: 0.5,
    particleCount: 200
  },
  hotAisle: {
    color: '#ff6600',      // 热气流(橙色)
    direction: 'up-out',
    speed: 0.8,
    particleCount: 150
  }
}
```

### 4.4 告警气泡

```
     ┌─────────────────┐
     │ ⚠️ 温度过高      │
     │ 当前: 32.5°C    │
     │ 阈值: 30°C      │
     │ 持续: 5分钟     │
     └────────┬────────┘
              │
              ▼
         [机柜模型]
```

## 5. 交互功能

### 5.1 视角控制

```typescript
interface CameraConfig {
  orbit: {
    enableRotate: true,
    enableZoom: true,
    enablePan: true,
    minDistance: 5,
    maxDistance: 100,
    minPolarAngle: 0.1,
    maxPolarAngle: Math.PI / 2.1
  },
  presets: {
    overview: { position: [0, 50, 50], target: [0, 0, 0] },
    moduleA:  { position: [20, 15, 20], target: [20, 0, 0] },
    topDown:  { position: [0, 80, 0], target: [0, 0, 0] }
  }
}
```

### 5.2 点击交互流程

```
用户点击机柜
    ├─→ Raycaster 射线检测命中对象
    ├─→ 高亮选中机柜 (OutlinePass 描边)
    ├─→ 相机平滑飞行到机柜前方
    └─→ 弹出详情面板
         ├── 机柜基本信息
         ├── 实时数据
         ├── 设备列表
         └── 历史曲线
```

### 5.3 深度穿透浏览

```
Level 0: 机房全景 → (双击) → Level 1: 模块特写
Level 1: 模块特写 → (双击) → Level 2: 机柜内部
Level 2: 机柜内部 → (双击) → Level 3: 设备详情面板
```

### 5.4 告警定位飞行

```typescript
async function flyToAlarm(alarmId: string) {
  const device = findDeviceByAlarm(alarmId)
  highlightDevice(device, 'alarm')
  await animateCamera({
    from: camera.position,
    to: calculateViewPosition(device),
    duration: 1.5,
    easing: 'easeInOutCubic'
  })
  showDeviceDetail(device)
}
```

## 6. 场景模式

### 6.1 模式对比

| 特性 | 指挥中心模式 | 运维模式 | 展示模式 |
|------|-------------|---------|---------|
| 视角 | 固定全景俯视 | 自由控制 | 自动巡航漫游 |
| 数据密度 | 全部显示 | 全部显示 | 仅核心指标 |
| 悬浮面板 | 全部展开 | 可折叠 | 最小化/隐藏 |
| 交互权限 | 只读 | 可操作控制 | 只读 |
| 刷新频率 | 5秒 | 3秒 | 10秒 |
| 特效 | 标准 | 简化 | 增强(辉光/景深) |

### 6.2 指挥中心模式

```typescript
const commandCenterMode = {
  camera: { locked: true, preset: 'overview', autoRotate: false },
  panels: {
    left: ['environment', 'alarmList'],
    right: ['energy', 'pue', 'demand'],
    top: ['status', 'time', 'kpi'],
    bottom: ['moduleSelector']
  },
  dataLayers: ['status', 'heatmap', 'alarms', 'power'],
  interaction: { clickDetail: true, control: false }
}
```

### 6.3 展示模式 - 自动巡航

```typescript
const showcaseMode = {
  camera: {
    autoTour: true,
    tourPath: [
      { position: [0, 50, 50], duration: 5000, pause: 3000 },
      { position: [30, 10, 20], duration: 4000, pause: 5000 },
      { position: [-20, 8, 15], duration: 4000, pause: 5000 },
      { position: [0, 30, 0], duration: 3000, pause: 2000 }
    ],
    loop: true
  },
  panels: { left: ['kpiCards'], right: [], bottom: ['companyLogo'] },
  effects: { bloom: true, depthOfField: true, particleDensity: 'high' }
}
```

### 6.4 模式切换动画

```
当前模式 → 目标模式
  ├─→ 1. 面板淡出动画 (300ms)
  ├─→ 2. 相机飞行到新预设位置 (800ms)
  ├─→ 3. 数据层切换 (立即)
  └─→ 4. 新面板淡入动画 (300ms)
```

## 7. 悬浮面板布局

### 7.1 顶部状态栏

```
┌────────────────────────────────────────────────────────────────────┐
│ 🕐 2026-01-19 15:32:45  │  XX数据中心机房  │ ⚡指挥中心 ▼ │ ⛶ │ PUE 1.45 │ 🔔 3 │
└────────────────────────────────────────────────────────────────────┘
```

### 7.2 左侧面板 - 环境监测

```
┌─────────────────────┐
│ 📊 环境监测    [−]  │
├─────────────────────┤
│ 温度概览            │
│ ┌─────────────────┐ │
│ │ 最高  32.5°C ▲ │ │
│ │ 平均  24.8°C   │ │
│ │ 最低  21.2°C   │ │
│ └─────────────────┘ │
├─────────────────────┤
│ 湿度概览            │
│ ┌─────────────────┐ │
│ │ 最高  65% ▲    │ │
│ │ 平均  48%      │ │
│ │ 最低  35%      │ │
│ └─────────────────┘ │
├─────────────────────┤
│ 🚨 实时告警 (3)     │
│ ┌─────────────────┐ │
│ │🔴 A-01 温度过高 │ │
│ │🟡 B-03 湿度告警 │ │
│ │🔴 UPS-1 负载高  │ │
│ └─────────────────┘ │
│     查看全部 →      │
└─────────────────────┘
```

### 7.3 右侧面板 - 能耗统计

```
┌─────────────────────┐
│ ⚡ 能耗统计    [−]  │
├─────────────────────┤
│ 实时功率            │
│    156.8 kW        │
│ IT: 98kW 制冷: 42kW │
├─────────────────────┤
│ PUE 趋势            │
│ ┌─────────────────┐ │
│ │  1.45  ▼0.02   │ │
│ │ [迷你折线图]    │ │
│ └─────────────────┘ │
├─────────────────────┤
│ 今日用电            │
│  1,250 kWh         │
│  电费: ¥875        │
├─────────────────────┤
│ 需量状态            │
│ ████████░░ 78%     │
│ 125/160 kW         │
└─────────────────────┘
```

### 7.4 底部控制栏

```
┌──────────────────────────────────────────────────────────────────────┐
│ 区域: [全部▼] [A区] [B区] [C区]  │  图层: ☑温度 ☑状态 ☑功率 ☐气流  │
│ 视角: [全景] [俯视] [漫游]  │  ▶ 巡航  │  ━━━━●━━━━━━ 时间轴        │
└──────────────────────────────────────────────────────────────────────┘
```

### 7.5 可拖拽面板系统 (V1.2)

所有悬浮面板现在支持拖拽移动和折叠功能：

#### 功能特性

| 功能 | 描述 |
|------|------|
| 拖拽移动 | 通过拖动面板标题栏可移动面板位置（展开或折叠状态均可拖拽） |
| 折叠/展开 | 点击折叠按钮可收起面板，保留半透明标题栏，隐藏内容区域 |
| 位置持久化 | 面板位置自动保存到 localStorage |
| 可见性控制 | 通过底部控制栏可切换面板显示/隐藏 |
| 重置布局 | 一键恢复所有面板到默认位置 |

#### 折叠行为 (V1.2 更新)

| 状态 | 显示效果 |
|------|----------|
| 展开 | 完整面板：标题栏 + 内容区域 |
| 折叠 | 保留半透明标题栏（保持原有宽度），内容区域隐藏 |

**设计原则：**
- 折叠时标题栏保持可见，用户可识别面板类型
- 折叠状态下面板仍可拖拽移动
- 使用 CSS transition 实现平滑动画
- 面板宽度在折叠时保持不变（不缩小）

#### DraggablePanel 组件

```vue
<DraggablePanel
  title="面板标题"
  panelId="panelIdentifier"
  :initialX="20"
  :initialY="60"
  :initialCollapsed="false"
  @positionChange="handlePositionChange"
  @collapseChange="handleCollapseChange"
>
  <!-- 面板内容 -->
</DraggablePanel>
```

#### 面板状态管理

```typescript
// Pinia store 中的面板状态
interface PanelState {
  x: number         // X 位置
  y: number         // Y 位置
  collapsed: boolean // 是否折叠
  visible: boolean   // 是否可见
}

panelStates: {
  leftPanel: { x: 20, y: 60, collapsed: false, visible: true },
  rightPanel: { x: -300, y: 60, collapsed: false, visible: true },  // 负值表示从右侧计算
  floorSelector: { x: 20, y: 120, collapsed: false, visible: true },
  deviceDetail: { x: -320, y: 60, collapsed: false, visible: true }
}
```

#### 底部控制栏面板管理

```
┌──────────────────────────────────────────────────────────────────────────────┐
│ 图层: ☑温度 ☑状态 ☑功率 ☐气流 │ 面板: ☑环境 ☑能耗 ☑楼层 [重置] │ 视角: [全景] [俯视] │
└──────────────────────────────────────────────────────────────────────────────┘
```

### 7.6 面板点击导航 (V1.1)

面板中的可点击区域支持导航到主系统对应页面：

| 面板区域 | 导航目标 | 打开方式 |
|---------|----------|---------|
| 实时功率 | `/energy/analysis` | 新标签页 |
| PUE 趋势 | `/energy/analysis` | 新标签页 |
| 今日用电 | `/energy/statistics` | 新标签页 |
| 需量状态 | `/energy/topology` | 新标签页 |
| 温度/湿度 | `/monitor` | 新标签页 |
| 告警列表 | `/alarm` | 新标签页 |

```typescript
// 导航始终在新标签页打开
function handleNavigate(path: string) {
  const baseUrl = window.location.origin
  const fullUrl = `${baseUrl}${path}`
  window.open(fullUrl, '_blank')
}
```

## 8. 文件结构

### 8.1 新增目录结构

```
frontend/src/
├── views/
│   └── bigscreen/
│       └── index.vue                 # 大屏主入口
│
├── components/
│   └── bigscreen/
│       ├── ThreeScene.vue            # Three.js 场景容器
│       ├── DataCenterModel.vue       # 机房3D模型组件
│       ├── CabinetModel.vue          # 机柜3D模型
│       ├── DeviceModel.vue           # 设备3D模型
│       ├── AirflowParticles.vue      # 气流粒子效果
│       ├── HeatmapOverlay.vue        # 热力图覆盖层
│       ├── AlarmBubble.vue           # 告警气泡
│       │
│       ├── panels/
│       │   ├── TopStatusBar.vue      # 顶部状态栏
│       │   ├── LeftPanel.vue         # 左侧环境面板
│       │   ├── RightPanel.vue        # 右侧能耗面板
│       │   ├── BottomControl.vue     # 底部控制栏
│       │   └── DeviceDetail.vue      # 设备详情弹窗
│       │
│       ├── ui/
│       │   ├── DraggablePanel.vue    # 可拖拽面板包装组件
│       │   ├── DigitalFlop.vue       # 数字翻牌组件
│       │   ├── ContextMenu.vue       # 右键菜单组件
│       │   └── ThemeSelector.vue     # 主题选择器
│       │
│       └── charts/
│           ├── MiniTrendChart.vue    # 迷你趋势图
│           └── GaugeRing.vue         # 环形仪表盘
│
├── composables/
│   └── bigscreen/
│       ├── useThreeScene.ts          # Three.js 场景管理
│       ├── useCameraControl.ts       # 相机控制
│       ├── useSceneMode.ts           # 场景模式切换
│       ├── useDataLayer.ts           # 数据图层管理
│       └── useAutoTour.ts            # 自动巡航控制
│
├── stores/
│   └── bigscreen.ts                  # 大屏状态管理
│
└── utils/
    └── three/
        ├── sceneSetup.ts             # 场景初始化
        ├── modelGenerator.ts         # 程序化模型生成
        ├── materialLibrary.ts        # 材质库
        └── animationHelper.ts        # 动画辅助函数
```

### 8.2 核心组件职责

| 组件 | 职责 |
|------|------|
| `ThreeScene.vue` | 初始化渲染器、场景、相机、灯光、后处理 |
| `DataCenterModel.vue` | 管理机房整体结构，加载/生成模块和机柜 |
| `CabinetModel.vue` | 单个机柜渲染，接收实时数据更新状态/颜色 |
| `useThreeScene.ts` | 提供场景实例、渲染循环、resize处理 |
| `useCameraControl.ts` | 封装轨道控制器、飞行动画、预设视角切换 |

## 9. 数据流与API集成

### 9.1 数据流架构

```
┌─────────────────────────────────────────────────────────────────┐
│                         大屏前端                                 │
├─────────────────────────────────────────────────────────────────┤
│   ┌─────────────┐    ┌─────────────┐    ┌─────────────┐        │
│   │ bigscreen   │───→│ composables │───→│ 3D Scene    │        │
│   │ store       │    │ useData*    │    │ Components  │        │
│   └─────────────┘    └─────────────┘    └─────────────┘        │
│          ↑                                                      │
│   ┌──────┴──────┐                                               │
│   │  WebSocket  │  (实时推送 + HTTP轮询兜底)                     │
│   └──────┬──────┘                                               │
└──────────┼──────────────────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────────────────────────────┐
│                       现有后端 API                               │
│  /api/v1/realtime/*    /api/v1/energy/*    /api/v1/alarms/*    │
└─────────────────────────────────────────────────────────────────┘
```

### 9.2 复用现有API

| 数据需求 | 现有API | 调用频率 |
|---------|---------|---------|
| 点位实时值 | `GET /realtime` | WebSocket/5s |
| 环境统计 | `GET /realtime/summary` | 10s |
| 告警列表 | `GET /alarms/active` | WebSocket/5s |
| PUE数据 | `GET /energy/pue` | 10s |
| 能耗统计 | `GET /energy/statistics/summary` | 30s |
| 需量状态 | `GET /energy/demand/15min-curve` | 60s |
| 配电拓扑 | `GET /energy/topology` | 首次加载 |

### 9.3 新增API

```typescript
// 获取机房3D布局配置
GET /api/v1/bigscreen/layout

interface DataCenterLayout {
  name: string
  dimensions: { width: number, length: number, height: number }
  modules: Array<{
    id: string
    name: string
    position: { x: number, z: number }
    rotation: number
    cabinetRows: Array<{
      id: string
      startPosition: { x: number, z: number }
      direction: 'x' | 'z'
      cabinetCount: number
      cabinetIds: string[]
    }>
    coolingUnits: Array<{ id: string, position: { x: number, z: number } }>
  }>
  infrastructure: {
    upsRoom: { position: { x: number, z: number }, size: {...} }
    powerRoom: { position: { x: number, z: number }, size: {...} }
  }
}
```

### 9.4 Pinia Store

```typescript
// stores/bigscreen.ts
export const useBigscreenStore = defineStore('bigscreen', {
  state: () => ({
    mode: 'command' as 'command' | 'operation' | 'showcase',
    layout: null as DataCenterLayout | null,
    deviceData: {} as Record<string, DeviceRealtimeData>,
    layers: { heatmap: true, status: true, power: true, airflow: false },
    selectedDevice: null as string | null,
    activeAlarms: [] as AlarmItem[]
  })
})
```

## 10. 实现计划

### 10.1 依赖安装

```bash
npm install three @types/three postprocessing gsap stats.js
```

### 10.2 实现阶段

| 阶段 | 内容 | 任务数 |
|------|------|--------|
| Phase 1 | 基础场景搭建 | 4 |
| Phase 2 | 机房模型生成 | 4 |
| Phase 3 | 数据可视化层 | 5 |
| Phase 4 | 交互功能 | 5 |
| Phase 5 | 悬浮面板 | 5 |
| Phase 6 | 场景模式 | 5 |
| Phase 7 | 优化与完善 | 4 |

### 10.3 详细任务分解

**Phase 1: 基础场景搭建**
- Task 1.1: ThreeScene.vue 基础容器
- Task 1.2: 场景/相机/灯光/渲染器初始化
- Task 1.3: 轨道控制器 + resize响应
- Task 1.4: 基础地板和环境

**Phase 2: 机房模型生成**
- Task 2.1: 程序化机柜生成器
- Task 2.2: 模块化布局系统
- Task 2.3: 基础设施模型
- Task 2.4: 布局配置API + 加载逻辑

**Phase 3: 数据可视化层**
- Task 3.1: 设备状态着色
- Task 3.2: 温度热力图
- Task 3.3: 功率标签显示
- Task 3.4: 气流粒子动画
- Task 3.5: 告警气泡组件

**Phase 4: 交互功能**
- Task 4.1: 点击选中 + 高亮
- Task 4.2: 相机飞行动画
- Task 4.3: 深度穿透
- Task 4.4: 告警定位功能
- Task 4.5: 视角预设切换

**Phase 5: 悬浮面板**
- Task 5.1: 顶部状态栏
- Task 5.2: 左侧环境面板
- Task 5.3: 右侧能耗面板
- Task 5.4: 底部控制栏
- Task 5.5: 设备详情弹窗

**Phase 6: 场景模式**
- Task 6.1: 模式状态管理
- Task 6.2: 指挥中心模式
- Task 6.3: 运维模式
- Task 6.4: 展示模式 + 自动巡航
- Task 6.5: 模式切换动画

**Phase 7: 优化与完善**
- Task 7.1: 后处理效果
- Task 7.2: 性能优化
- Task 7.3: 路由集成 + 全屏支持
- Task 7.4: 测试与调试

### 10.4 新增路由

```typescript
// router/index.ts
{
  path: '/bigscreen',
  name: 'Bigscreen',
  component: () => import('@/views/bigscreen/index.vue'),
  meta: {
    title: '数字孪生大屏',
    fullscreen: true,
    requiresAuth: true
  }
}
```

## 11. 附录

### 11.1 参考资源

- Three.js 官方文档: https://threejs.org/docs/
- GSAP 动画库: https://greensock.com/gsap/
- ECharts GL: https://echarts.apache.org/zh/option-gl.html

### 11.2 设计决策记录

| 决策 | 选项 | 理由 |
|------|------|------|
| 3D框架 | Three.js 自研 | 灵活度最高，完全定制 |
| 模型方案 | 混合(程序化+精细模型) | 快速迭代，后续可升级 |
| 布局方式 | 3D全屏+悬浮面板 | 科技感强，沉浸体验 |
| 交互深度 | 完整交互 | 满足多场景需求 |
